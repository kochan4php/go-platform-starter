extends: default

rules:
  document-start: disable
  line-length: disable
  truthy:
    allowed-values: ["true", "false", "on"]
  comments:
    min-spaces-from-content: 1
  # Helm, Ansible, Compose, and GitHub expressions intentionally use {{ value }}.
  braces: disable
