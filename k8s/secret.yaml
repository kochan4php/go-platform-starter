apiVersion: v1
kind: Secret
metadata:
  name: express-ts-secret
type: Opaque
# TEMPLATE ONLY — replace every CHANGE_ME value with real secrets from your
# secret manager before applying (e.g. Sealed Secrets, External Secrets, SOPS).
# Never commit real values to this repository.
stringData:
  DATABASE_URL: "postgresql://CHANGE_ME:CHANGE_ME@postgres:5432/expressts"
  ACCESS_TOKEN_SECRET: "CHANGE_ME"
  REFRESH_TOKEN_SECRET: "CHANGE_ME"
  SESSION_TOKEN_SECRET: "CHANGE_ME"
