get:
  summary: API index
  tags: [Core]
  security: []
  responses:
    '200':
      description: Welcome payload
      content:
        application/json:
          schema:
            type: object
            properties:
              success: { type: boolean, example: true }
              message: { type: string }
              data:
                type: object
                properties:
                  docs: { type: string, format: uri }
