get:
  summary: Liveness probe
  tags: [Health]
  security: []
  responses:
    '200':
      description: Service is up
      content:
        application/json:
          schema:
            $ref: '../components/schemas/Envelope.yaml#/Envelope'
