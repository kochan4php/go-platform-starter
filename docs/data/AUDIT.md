# Schema governance audit

> Generated by `go run ./cmd/dbdocs`; findings are review inputs, not automatic destructive actions.

## Columns without comments (0)

None.

## Required columns without defaults (26)

- `audit.audit_logs.action`
- `audit.audit_logs.entity`
- `audit.event_outbox.id`
- `audit.event_outbox.stream`
- `audit.event_outbox.event`
- `audit.event_outbox.payload`
- `audit.processed_messages.message_id`
- `auth.change_log.table_name`
- `auth.change_log.operation`
- `auth.sessions.user_id`
- `auth.sessions.refresh_token_hash`
- `auth.sessions.family_id`
- `auth.sessions.expires_at`
- `rbac.change_log.table_name`
- `rbac.change_log.operation`
- `rbac.permissions.name`
- `rbac.role_permissions.role_id`
- `rbac.role_permissions.permission_id`
- `rbac.roles.name`
- `rbac.user_roles.user_id`
- `rbac.user_roles.role_id`
- `rbac.user_versions.user_id`
- `users.change_log.table_name`
- `users.change_log.operation`
- `users.users.email`
- `users.users.password_hash`

## Time columns not using TIMESTAMPTZ (0)

None.

## ck_/fk_/uq_ naming exceptions (0)

None.
