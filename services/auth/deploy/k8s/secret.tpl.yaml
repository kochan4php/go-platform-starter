# Copy to a real Secret (or render via your secrets tooling) — placeholders only.
apiVersion: v1
kind: Secret
metadata:
  name: auth
type: Opaque
stringData:
  PORT: "8080"
  LOG_LEVEL: info
  DATABASE_URL: postgres://auth:PASS@postgres:5432/app?sslmode=disable
  REDIS_ADDR: redis:6379
  REDIS_USERNAME: auth
  REDIS_PASSWORD: replace-from-secret-store
  ACCESS_TOKEN_SECRET: change-me-min-16-chars!!
  APP_PUBLIC_URL: https://app.example.com
  COOKIE_SECURE: "true"
