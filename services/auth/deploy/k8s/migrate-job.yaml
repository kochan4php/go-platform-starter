# Runs golang-migrate before every rollout (PLAN item 8). The deployment's
# readyz verifies the applied version instead of migrating on boot, so
# scale-out events never race migrations.
apiVersion: batch/v1
kind: Job
metadata:
  name: auth-migrate
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app: auth-migrate
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      restartPolicy: Never
      containers:
        - name: migrate
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
          image: ghcr.io/kochan4php/auth:latest-migrate
          envFrom:
            - secretRef:
                name: auth
          env:
            - name: MIGRATIONS_DIR
              value: /migrations
