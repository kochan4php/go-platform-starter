# Copy to a real Secret (or render via your secrets tooling) — placeholders only.
apiVersion: v1
kind: Secret
metadata:
  name: worker
type: Opaque
stringData:
  PORT: "8080"
  LOG_LEVEL: info
  DATABASE_URL: postgres://worker:PASS@postgres:5432/app?sslmode=disable
  REDIS_ADDR: redis:6379
  MAILER_DRIVER: smtp
  SMTP_HOST: smtp.example.com
  SMTP_PORT: "587"
  SMTP_USER: apikey
  SMTP_PASS: change-me
  MAIL_FROM: noreply@example.com
  MAIL_FROM_NAME: Platform
